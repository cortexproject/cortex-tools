run:
  timeout: 10m
  modules-download-mode: vendor
  build-tags:
    - netgo
    - require_docker

output:
  formats:
    - format: line-number

linters:
  disable-all: true
  enable:
    - goimports
    - revive
    - misspell
    - gofmt

linters-settings:
  goimports:
    local-prefixes: "github.com/cortexproject/cortex-tools"
